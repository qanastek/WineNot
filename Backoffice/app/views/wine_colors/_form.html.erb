<%= form_with(model: wine_color, local: true) do |form| %>
  <% if wine_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wine_color.errors.count, "error") %> prohibited this wine_color from being saved:</h2>

      <ul>
        <% wine_color.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
